syntax = "proto3";

package gb_mahjong.engine.v1;

// 正式的 gRPC service 定义目前仍保留在 proto/internal_engine.proto。
// 这样做是为了继续生成稳定的 internal_engine.grpc.pb.h，
// 避免影响当前 C++ 规则引擎目标的 include 路径。
// 本文件先保留目录位置，等未来不再依赖该兼容性时再做彻底拆分。
